# =============================================================================
# FinOps Cost Data Analyst - Environment Configuration
# =============================================================================

# Google GenAI API Configuration
GOOGLE_API_KEY=your-google-api-key-here

# Google Cloud Configuration (for BigQuery via service account)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# =============================================================================
# BigQuery Configuration - DYNAMIC TABLE DISCOVERY
# =============================================================================
# The agent automatically discovers datasets and tables in your project.
# Only BIGQUERY_PROJECT is required - the rest are optional fallback hints.

# REQUIRED: Your GCP Project ID
BIGQUERY_PROJECT=your-bigquery-project-id   # e.g., gac-prod-471220

# OPTIONAL: Fallback hints (used if dynamic discovery fails)
BIGQUERY_DATASET=agent_bq_dataset           # Fallback cost dataset name
BIGQUERY_TABLE=cost_analysis                # Fallback cost table name

# =============================================================================
# MULTI-TABLE FINOPS SETUP (Recommended Dataset/Table Names)
# =============================================================================
# The agent will automatically discover these based on pattern matching.
# Create your BigQuery datasets/tables with these naming conventions:
#
# 1. COST ANALYSIS (Actual Spending):
#    Recommended Dataset Names: cost_dataset, agent_bq_dataset, costs, spending
#    Recommended Table Names: cost_analysis, cost_data, costs
#    Example: `gac-prod-471220.cost_dataset.cost_analysis`
#    Schema: date, cto, cloud, application, managed_service, environment, cost
#
# 2. BUDGET (Allocations & Forecasts):
#    Recommended Dataset Names: budget_dataset, budgets, financial_planning
#    Recommended Table Names: budget, budget_allocations, forecasts
#    Example: `gac-prod-471220.budget_dataset.budget`
#    Schema: date, application, budget_amount, fiscal_year, department
#
# 3. USAGE (Resource Utilization):
#    Recommended Dataset Names: usage_dataset, resource_usage, utilization
#    Recommended Table Names: usage, resource_usage, consumption
#    Example: `gac-prod-471220.usage_dataset.resource_usage`
#    Schema: date, resource_type, application, usage_hours, usage_amount
#
# The agent uses intelligent pattern matching on dataset/table names to
# automatically route queries to the correct data source.
# =============================================================================

# Model Configuration
ROOT_AGENT_MODEL=gemini-2.0-flash-exp
SQL_GENERATOR_MODEL=gemini-2.0-flash-exp
TEMPERATURE=0.01

# Logging
LOG_LEVEL=INFO
